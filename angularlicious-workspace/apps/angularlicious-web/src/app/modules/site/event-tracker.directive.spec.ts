import { EventTrackerDirective } from './event-tracker.directive';

describe('EventTrackerDirective', () => {
  it('should create an instance', () => {
    const directive = new EventTrackerDirective();
    expect(directive).toBeTruthy();
  });
});
